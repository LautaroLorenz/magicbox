<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:orientation="vertical"
    android:padding="20dp">

    <Button
        android:id="@+id/btn_conectar"
        android:layout_width="125dp"
        android:layout_height="wrap_content"        
        android:layout_centerInParent="true"
        android:layout_alignParentRight="true"
        android:text="Conectarse" />
    
    <TextView
        android:id="@+id/paired_device_name"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_toLeftOf="@+id/btn_conectar"
        android:layout_alignTop="@+id/btn_conectar"
        android:layout_alignParentLeft="true"
        android:textSize="16sp"
        android:textColor="#000000"
        android:text="Galaxy Nexus" />
	
    <TextView
        android:id="@+id/paired_device_adress"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_toLeftOf="@+id/btn_conectar"
        android:layout_alignParentLeft="true"
        android:layout_alignBottom="@+id/btn_conectar"
        android:text="000000000" />
    
</RelativeLayout>