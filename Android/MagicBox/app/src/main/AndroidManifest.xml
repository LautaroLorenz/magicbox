<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.magicbox.magicbox"
    android:versionCode="1"
    android:versionName="1.0.0">
    <!-- - Aca se indican los permisos que va a necesitar la aplicacion -->
    <uses-permission android:name="android.permission.BLUETOOTH" />
    <uses-permission android:name="android.permission.BLUETOOTH_ADMIN" />
    <uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION" />
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" />
    <uses-permission android:name="android.permission.READ_PHONE_STATE" />
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE" />

    <application
        android:allowBackup="true"
        android:label="@string/app_name"
        android:theme="@style/AppTheme">
        <activity android:name=".activities.HistorialActivity"></activity>
        <activity android:name=".activities.PairedDeviceListActivity" /> <!-- - Defino la activity Principal -->
        <activity
            android:name=".activities.MainActivity"
            android:label="@string/app_name">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".BluetoothActivity"
            android:label="Bluetooth">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity> <!-- - Defino la activity que se comunica, transfierendo datos de los sensores, con Arduino -->
        <activity android:name=".activity_comunicacion" />
        <activity android:name=".activities.ProductoActivity" />
        <activity android:name=".activities.ProductListActivity" />
        <activity
            android:name=".activities.DeviceListActivity"
            android:label="Device List">
            <intent-filter>
                <action android:name="android.intent.action.VIEW" />
            </intent-filter>
        </activity>
    </application>

</manifest>